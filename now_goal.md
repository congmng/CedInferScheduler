当前背景如下：
随着llm模型的不断演化，kv大小随token长度比例不断减小，最新模型研究比如ds已经能够将kv压缩到很小的程度，从而让跨域环境下（<1GB/S）kv传输相比重算有可能能够取得收益,进而在跨域环境下异构pd分离有了可调度的空间

计划搭建问题：

1.在一个多域异构环境下，pd之间的请求要如何进行匹配，之前研究主要是基于pd的域内简单成比例分配，但在跨域环境下可能请求路由比例不同需要求解，同时pd之间的对应关系也不是简单按比例对应
2.完成1后，对于某种特定请求负载特征，我们已经可以求解出指定的pd匹配策略了，但是这是个静态解，同样会陷入既然负载规律相同，那么按照最优比例扩缩容就好了的问题，体现不出算法的在线意义，因此进一步扩充背景，希望请求负载具有多种特征，他们在时间轴上的特点不同，这样就可以让调度算法和当前集群状态产生关联，从而变成在线算法
3.在2的基础上，每次通过算法求接完当前最优pd分配策略后，考虑每次增加/减少X个p/d节点，是否能够带来显著的优化，比如缩容后整体性能变化不大，或者扩容后可以更好的提升性能，这里之所以不直接一次性求解新最优解，是因为最优解可能需要对集群进行大规模修改，可能会带来很大的切换开销，因此采用这种渐近的方式，这样就体现了动态扩缩容的部分
4.存算协同部分主要体现方式为kv的跨域迁移成本计算，pd缩容时会先冻结，等pd排空后再进行统一扩缩容，p缩容时会额外考虑是否迁移/怎么迁移其上的kv
5.对于比例路由的结果仍然需要考虑对于不同请求的kv亲和来进行具体调度，并考虑最小化因为kv重算导致的总体损失


测试方式：
考虑在模拟器上进行，先在各个异构显卡上收集profile，然后在模拟其中进行对应模拟
模拟环境下域内速度为1GB/S，跨域速度为100MB/S-400MB/s不等
这里的d节点不会再做p，防止影响其他d请求，重算优势会体现在在本地另外起p节点上


数据集：
考虑使用拼合数据集，数据集采用公开数据集修改进行，请求要额外包含slo，到达频率等信息